1. Crie um arquivo chamado .env.dev na pasta raiz

2. Dentro de .env.dev adicione as variaveis de ambiente (eg. .examplo.env)

3. Execute o seguinte comando docker-compose --env-file .env.dev up --build